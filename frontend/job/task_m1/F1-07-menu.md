# F1-07：侧边栏菜单声明数据 menu

## Agent 角色

目录配置专项 Agent — **仅维护树状目录声明式数据（唯一目录配置源）**。

## 唯一负责文件

```
src/layout/menu.js
```

## 禁止修改

- `SidebarTree.vue`、`layout/index.vue`、`router/index.js` 及其他文件

## 前置依赖

- 无（与路由树约定一致，见 README §2.1）

## 开发要求

### 1. 导出 `menuTree`

两级树结构，节点形如 `{ title, icon?, path?, children? }`：

- 一级即叶子：`总览驾驶舱` → `/dashboard`
- 分组「日志监控」：7 个监控子页叶子
- 分组「智能分析」：异常诊断中心/周期体检报告/预警中心/调用链路追踪/业务漏斗洞察
- 分组「系统运维」：链路健康与验证/组件运行状态/配置快照

`path` 必须与 README §2.1 路由表逐一对应。

### 2. 约束

- 纯声明数据，不含渲染逻辑（渲染归 F1-08）。
- **新增页面只改本文件与路由**，不改布局模板。
- 简体中文标题。

## 验收标准

| 编号 | 验收项 | 通过条件 |
| --- | --- | --- |
| AC-01 | 结构 | 1 个一级叶子 + 3 个分组，分组下叶子齐全（7+5+3） |
| AC-02 | path 对齐 | 全部 path 与路由表一致 |
| AC-03 | 纯数据 | 不含组件/渲染逻辑 |

## 完成定义（DoD）

- [ ] 仅修改 `src/layout/menu.js`
- [ ] 不修改 DEV.md

## 下游消费说明（供其他 Agent 只读）

- F1-08 SidebarTree 渲染 `menuTree`
